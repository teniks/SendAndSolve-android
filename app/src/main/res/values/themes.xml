<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SendAndSolve" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SendAndSolve" parent="Base.Theme.SendAndSolve" />

    <style name="outlined_button" parent="android:Widget.Button">
        <item name="android:background">@drawable/rounded_outline_button</item>
        <item name="android:backgroundTint">@color/accent_1</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">8dp</item>
        <item name="cornerRadius">15dp</item>
    </style>

    <style name="circle_button" parent="android:Widget.Button">
        <item name="android:background">@drawable/circle_button</item>
        <item name="android:backgroundTint">@color/accent_1</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="rounded_rectangle" parent="android:Widget.Button">
        <item name="android:background">@drawable/rounded_rectangle</item>
        <item name="android:backgroundTint">@color/accent_1</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="circle_imageview">
        <item name="cornerFamily">rounded</item>
        <item name="strokeColor">@color/white</item>
        <item name="strokeWidth">3dp</item>
        <item name="cornerSize">50%</item>
    </style>


</resources>